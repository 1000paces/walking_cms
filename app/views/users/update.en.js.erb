$(".wcms-master-toolbar-link").removeClass("active");
$("#wcms-form-box").hide();
if($("#wcms-drawer-account").is(':visible')){
	$("#wcms-drawer-account").hide("slide", { direction: "right" }, 500);
	$("#drawer-forms").empty();
	$("#wcms-wrapper").removeClass("drawer-visible");
}