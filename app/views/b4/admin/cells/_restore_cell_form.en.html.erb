<%= form_tag admin_cell_restore_path, :id => "wcms-restore-cell", :class => "form-inline", :remote => true do |f| -%>
	<%= hidden_field_tag("page_id", page.id, {:id => "wcms-restore-page-id"}) %>
	<%= hidden_field_tag("row_id", row.id, {:id => "wcms-restore-row-id"}) %>
	<%= hidden_field_tag("cell_id", cell.id, {:id => "wcms-restore-cell-id"}) %>
<%- end -%>