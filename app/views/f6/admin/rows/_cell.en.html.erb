<div class="<%= cell.grid_style %> sort wcms-sort" id="wcms-cell-<%= cell.id %>" data-width="<%= cell.width %>" data-offset="<%= cell.offset %>" data-row="<%= row.id -%>" data-cell="<%= cell.id -%>">
	<div class="wcms-cell-sort-border" title="row <%= row.id -%> | cell <%= cell.id -%>">
		<%= render :partial => "shared/#{cell.gauge}", :locals => {row: row, cell: cell} -%>	
	</div>
</div>