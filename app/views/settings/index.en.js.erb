$(".wcms-master-toolbar-link").removeClass("active");
if($("#drawer-settings").is(':visible')){
	$("#drawer-settings").hide("slide", { direction: "right" }, 500);
	$("#drawer-forms").empty();
	$("#wcms-wrapper").removeClass("toggled");
} else {
	$("#settings-index-div").html("<%= j render(:partial => 'settings/index') -%>");
	$(".wcms-right-column").hide(); 
	$("#wcms-wrapper").addClass("toggled");
	$("#wcms-toggle-settings").addClass("active");
	$("#drawer-settings").fadeIn(); /*show("slide", { direction: "right" }, 150);*/
}